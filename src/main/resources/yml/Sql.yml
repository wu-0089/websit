spring:
  datasource:
    #url: jdbc:mysql://${DB_HOST}:3306/${DB_NAME}
    #username: ${DB_USERNAME}
    #password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true